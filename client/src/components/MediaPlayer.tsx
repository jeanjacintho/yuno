export function MediaPlayer() {
  return (
    <div className="mt-8 rounded-xl border border-slate-800 bg-slate-900 p-4">
      {/* <video> nativo com src apontando para /api/stream/:chatId/:messageId */}
      <p className="text-sm text-slate-500">Player de vídeo / viewer de PDF</p>
    </div>
  )
}
